meta {
  name: Create List
  type: http
  seq: 2
}

post {
  url: {{apiUrl}}/lists
  body: json
  auth: none
}

body:json {
  {
    "name": "Groceries",
    "description": "Weekly shopping list"
  }
}

script:post-response {
  let data = res.body;
  bru.setEnvVar("listId", data.id);
}
