meta {
  name: Update Task
  type: http
  seq: 4
}

patch {
  url: {{apiUrl}}/tasks/:taskId
  body: json
  auth: none
}

params:path {
  taskId: {{taskId}}
}

body:json {
  {
    "completed": true,
    "priority": "high"
  }
}
