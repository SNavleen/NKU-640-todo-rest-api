meta {
  name: Update Task
  type: http
  seq: 4
}

patch {
  url: {{apiUrl}}/tasks/:taskId
  body: json
  auth: none
}

params:path {
  taskId: REPLACE_WITH_ACTUAL_TASK_ID
}

body:json {
  {
    "completed": true,
    "priority": "high"
  }
}
