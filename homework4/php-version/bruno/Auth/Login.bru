meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{apiUrl}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "username": "testuser",
    "password": "password123"
  }
}

script:post-response {
  let data = res.body;
  bru.setEnvVar("token", data.token);
}
